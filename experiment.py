from macros import *
import numpy as np
import grid_agent as ga
from world import world
from visualize import visualize

class experiment:
    def __init__(self, world_dim, nagents, agent_xy):
        self.world = world(world_dim)
        for index in range(nagents):
            

if __name__ == "__main__":
